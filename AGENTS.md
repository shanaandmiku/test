# 项目说明
- 这是一个纯前端应用，不要引入后端运行时、常驻服务进程或数据库依赖。
- 更新代码时，默认不需要考虑旧状态、旧数据格式或历史运行行为的兼容性，除非用户明确提出相关要求。
- 目前不进行i18n国际化。所有文字、注释优先使用中文。

# 编码规范
- 禁用 JS 只用 TypeScript
- 禁用 any 可以用 unknown
- 文件名全小写用 - 连接。
- `.vue` 模板中的组件标签名和属性名优先使用PascalCase 或 camelCase 的模板写法，除非框架语法本身强制要求。
- css样式优先使用vue模板文件的style scoped标签控制，避免全局污染。
- 全局样式仅做通用变量、样式重置使用
- 文档、注释等描述性用语使用中文。
- 函数级别的代码和作用域比较大的变量需要添加简单描述，但不需要添加参数描述，除非参数比较复杂。
- 简易注释使用//而非/**/，带有复杂参数或者功能复杂的函数才使用/**/的注释
- 样式无复用时优先使用内联样式

# AI功能限制
- 运行命令时直接使用全局命令，不要显式套一层 shell 可执行文件；例如直接用 `git`、`npm`、`node`，不要再手动指定 `pwsh`、`powershell`、`bash`、`zsh` 等外层解释器来包裹这些命令。
- 如果用户要求“提交”，且没有明确说明只做本地提交，则默认执行“提交并推送到当前远端分支”，AGENTS.md始终要提交。
- 优先进行web文档查询而非进行源代码搜索，js源代码难以阅读
- 禁止使用删除、移动、重命名操作，如有需求，将源文件复制到项目根目录Trashcan文件夹下重命名为‘源文件名.删除时间戳’的格式然后交给用户，让用户自行抉择是否删除