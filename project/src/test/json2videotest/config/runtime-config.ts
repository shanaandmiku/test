import type { Json2VideoRuntimeConfig } from '../type/type-a-b.ts'

// 当前实验的运行配置
export const testRuntimeConfig: Json2VideoRuntimeConfig = {
  background: 0x04070d,
  viewport: {
    height: 720,
    width: 1280,
  },
}
